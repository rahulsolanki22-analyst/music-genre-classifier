To run the Music Genre Classifier locally:

1. Install dependencies:
   pip install -r requirements.txt

2. Run the Streamlit app:
   streamlit run app.py

3. Open browser and go to:
   http://localhost:8501